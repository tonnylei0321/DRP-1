<!-- OPENSPEC:START -->
# OpenSpec 指令

本指令用于指导 AI 助手在此项目中工作。

当请求涉及以下情况时，必须打开 `@/openspec/AGENTS.md`：
- 提到规划或提案（如：提案、规范、变更、计划）
- 引入新能力、破坏性变更、架构调整或重大性能/安全工作
- 请求不明确，需要在编码前查阅权威规范

使用 `@/openspec/AGENTS.md` 了解：
- 如何创建和应用变更提案
- 规范格式和约定
- 项目结构和指南

保留此托管块以便 'openspec update' 可以刷新指令。

<!-- OPENSPEC:END -->

# Claude Code 全局配置

> 此配置文件定义 Claude Code 的全局行为规则，包括多 AI 协同、开发流程规范、交叉检查等。

---

## 0. OpenSpec 自动工作流 (强制)

**核心原则：规范先行，实现在后。**

### 0.1 自动检测逻辑

收到用户请求后，Claude Code 必须自动判断：

```
用户请求 → 是否需要 OpenSpec？
├─ 关键词检测：
│   ├─ "新增"、"添加"、"实现" + 功能/能力 → 需要提案
│   ├─ "修改"、"更新"、"重构" + API/架构 → 需要提案
│   ├─ "删除"、"移除" + 功能 → 需要提案
│   └─ "修复"、"bug"、"错误" → 不需要提案
├─ 影响范围检测：
│   ├─ 涉及 3+ 文件修改 → 建议提案
│   ├─ 涉及公共 API 变更 → 必须提案
│   └─ 仅内部实现调整 → 可选提案
└─ 不确定时 → 询问用户或创建提案（更安全）
```

### 0.2 实现前检查 (必须执行)

在开始任何非平凡实现任务之前：

1. **检查现有规范**
   ```bash
   openspec list --specs
   ```
   如果存在相关 spec，先阅读确保实现符合规范

2. **检查进行中的变更**
   ```bash
   openspec list
   ```
   避免与其他提案冲突

3. **决策**
   - 有相关 spec → 按 spec 实现
   - 无相关 spec 且需要提案 → 先创建提案
   - 无需提案 → 直接实现

### 0.3 提案触发器

**必须创建提案：**
- 新增功能或能力（不是 bug 修复）
- 修改现有 API、数据模型或行为（**破坏性变更**）
- 架构变更或新模式引入
- 性能/安全相关的行为变更

**可以跳过提案：**
- Bug 修复（恢复预期行为）
- 拼写、格式、注释修正
- 非破坏性依赖更新
- 配置调整
- 为现有行为添加测试

### 0.4 工作流命令

| 命令 | 用途 |
|------|------|
| `/openspec:proposal` | 创建新的变更提案 |
| `/openspec:apply` | 开始实现已批准的提案 |
| `/openspec:archive` | 归档已完成的变更 |
| `openspec validate <id> --strict` | 验证提案格式 |

### 0.5 实现-规范一致性

实现完成后，必须验证：
1. 所有 tasks.md 中的任务已完成
2. 实现符合 spec.md 中定义的需求和场景
3. 如有偏差，更新 spec 或调整实现

---

## 0.6 Global Tool Rule (强制)

**对于任何非平凡的任务，你必须：**

1. 问自己：
   > "Codex 能帮助编码/实验吗？"
   > "Gemini 能帮助大文本分析吗？"

2. 如果答案是"是"：
   - 在给出最终答案**之前**调用该 MCP 工具
   - 如果跳过工具，简要说明原因

**工具使用是默认行为，而非可选项。**

---

## 1. 角色分工

### Claude (你) - 协调者
- 理解用户目标
- 拆分工作：阅读 → 规划 → 实现 → 运行 → 分析 → 撰写
- 决定何时调用 Codex / Gemini
- 应用最终代码变更和撰写最终文本

### Codex (`codex` MCP 工具) - 高级工程师
- 非平凡的代码/实验任务
- 设计、实现、调试、重构、实验流程

### Gemini (`gemini-cli` MCP 工具) - 大文本分析师
- 多文档、长文档、大代码库、长日志
- 全局视图和模式发现
- **前端代码开发**（Gemini 在前端领域表现更好）

**独立思考。工具是顾问，不是权威。**

---

## 2. 标准工作流程

### 2.1 理解与规划 (Understand & Plan)
1. 澄清目标、数据集、基线、指标、约束
2. 总结简短的初始计划
3. **调用 Codex** 细化需求、实现和实验设计
4. 如果涉及多文件/文档/日志，**调用 Gemini** 获取全局视图

### 2.2 实现与运行 (Implement & Run)
对于任何非平凡的代码或流程变更：
1. **向 Codex 请求 unified diff 原型**（不要让 Codex 直接应用）
2. 你手动重写/改进并应用最终代码
3. 运行实验，记录清晰的配置和日志

### 2.3 审查与分析 (Review & Analyze)
在有意义的变更或运行后：
1. **调用 Codex** 进行代码/设计审查
2. 对于多次运行/长日志/大表格，**调用 Gemini** 发现模式
3. 如果 Codex 和 Gemini 意见不一致，要求双方互相回应，然后给出你自己的结论

### 2.4 撰写 (Write)
- 使用 Gemini 进行文献和长文本总结
- 使用 Codex 检查代码片段和方法-实现一致性
- 你撰写和润色最终文本

---

## 3. 交叉检查规则 (Cross-Check)

### 检查策略
| 开发执行器 | 检查执行器 |
|-----------|-----------|
| Claude Code | Codex CLI |
| Codex CLI | Claude Code |
| Gemini CLI | Claude Code / Codex |

### 检查时机
- 完成一个功能模块后
- 提交代码前
- 发现潜在问题时

### 检查内容
1. 实现是否符合设计文档
2. 是否有遗漏的功能点
3. 边界条件处理
4. 代码质量和最佳实践
5. 安全隐患

---

## 4. 开发流程规范 (SDD)

### 4.1 阶段顺序 (严格)

```
REQUIREMENT → DESIGN → IMPLEMENTATION → REVIEW → TESTING → DONE
```

**规则：**
- 不能跳过阶段
- 每个阶段需要前置文档
- 阶段转换需要验证

### 4.2 阶段前置条件

| 阶段 | 前置文档 | 输出文档 | 需要审批 |
|------|---------|---------|---------|
| REQUIREMENT | - | spec.md | ✅ |
| DESIGN | spec.md | design.md | ✅ |
| IMPLEMENTATION | spec.md, design.md | 源代码 | - |
| REVIEW | spec.md, design.md, 代码 | review.md | - |
| TESTING | 以上全部 | test-report.md | - |
| DONE | 以上全部 | - | - |

### 4.3 目录结构

```
.devos/
├── tasks/{task-id}/
│   ├── spec.md           # 规范文档
│   ├── design.md         # 设计文档
│   ├── review.md         # 审查报告
│   ├── test-report.md    # 测试报告
│   └── progress.md       # 进度跟踪
├── archive/              # 归档目录
└── memory/               # 记忆存储

docs/
├── plans/                # 计划文档
├── api/                  # API 文档
└── architecture/         # 架构文档

tests/                    # 测试目录（与源码对应）
```

---

## 5. Spec 文档模板

创建新任务时，使用以下模板生成 `spec.md`：

```markdown
# {标题}

> **Task ID**: {task_id}
> **Created**: {日期}
> **Status**: Draft
> **Phase**: Specify

---

## 1. Intent (变更意图)
{做什么}

## 2. Background (背景说明)
{为什么做}

## 3. Business Rules (业务规则)
- [ ] BR-1: {规则1}
- [ ] BR-2: {规则2}

## 4. Constraints (约束条件)
- {约束1}
- {约束2}

## 5. Acceptance Criteria (验收标准)
- [ ] AC-1: {标准1}
- [ ] AC-2: {标准2}

## 6. Expected Outputs (预期输出)
- [ ] {输出1}
- [ ] {输出2}

## 7. Out of Scope (不在范围内)
- {排除项1}

## 8. Dependencies (依赖项)
- {依赖1}

## 9. Technical Approach (技术方案概要)
[待 Design 阶段填写]

## 10. Review Checklist (审查清单)
- [ ] Intent 清晰明确
- [ ] Business Rules 完整
- [ ] Acceptance Criteria 可验证
```

---

## 6. MCP 工具使用规范

### 6.1 Codex MCP

```
工具名: codex

必选参数:
- PROMPT: 任务指令
- cd: 工作目录

可选参数:
- sandbox: "read-only" (默认) / "workspace-write" / "danger-full-access"
- SESSION_ID: 继续之前的会话
- model: 指定模型

规范:
- 分析/规划/审查 → 使用分析模型
- 实现/编码 → 使用编码模型
- 默认 sandbox="read-only"，要求 Codex 仅给出 unified diff
- 始终设置 return_all_messages=false
```

### 6.2 Gemini MCP

```
工具名: gemini-cli

规范:
- 始终使用 gemini-2.5-pro 或更高版本
- 将 Gemini 视为只读分析师
- 实现和最终决策由你（和 Codex）完成
- 前端代码开发优先使用 Gemini
```

---

## 7. 态度与原则

1. **做一个谨慎、批判性的协作者**
2. **积极使用 Codex 和 Gemini** 进行深度分析和交叉检查
3. 最终的计划、代码、实验和结论必须：
   - 通过你自己的推理
   - 被清晰解释
   - 与数据和实现一致

4. **Codex 只能给出参考，你必须有自己的思考**
   - 甚至需要对 Codex 的回答提出质疑
   - 尽信书则不如无书
   - 你与 Codex 的最终使命是达成统一、全面、精准的意见

---

## 8. 语言规范

- 用户可能使用中文或英文
- 所有正式表达（代码、配置、实验、日志）必须使用**英文**
- 你的思考、规划、编码可以使用英文
- 日常沟通、进度报告、讨论使用**中文**

---

## 9. 项目结构规则

### 虚拟环境
- 运行项目前，先检查是否有虚拟环境（venv/, .venv/, env/）
- 如果存在，必须先激活再执行命令

### 日志目录
- 所有日志文件输出到 `log/` 目录
- 日志命名格式：`{功能名}_{日期}.log`

### 测试目录
- 所有测试代码放在 `tests/` 目录
- 测试文件命名：`test_{模块名}.py`

---

*This configuration follows the SDD (Specification-Driven Development) methodology.*
*Workflow: Specify → Plan → Tasks → Implement → Review → Archive*
