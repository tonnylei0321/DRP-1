# 图任务模型评测报告

**生成时间**: 2026-02-06T12:20:43.214311

**评测样本数**: 54

**评测模型**: graph_rl, graph_sft


## 1. 总体评测结果

| 模型 | 正确数 | 总数 | 准确率 | 平均延迟 |
|------|--------|------|--------|----------|
| graph_rl | 43 | 54 | 79.6% | 37.27s |
| graph_sft | 31 | 54 | 57.4% | 0.53s |

## 2. 各任务类型准确率

| 任务类型 | graph_rl 准确率 | graph_sft 准确率 |
|----------|----------|----------|
| BipartiteMatching | 0/3 (0%) | 0/3 (0%) |
| COMMON_NEIGHBOR | 3/3 (100%) | 3/3 (100%) |
| EDGE | 3/3 (100%) | 3/3 (100%) |
| MST | 3/3 (100%) | 0/3 (0%) |
| NEIGHBOR | 2/3 (67%) | 2/3 (67%) |
| PREDECESSOR | 3/3 (100%) | 1/3 (33%) |
| SHORTEST_PATH | 1/3 (33%) | 1/3 (33%) |
| cycle_detection | 2/3 (67%) | 1/3 (33%) |
| description | 1/3 (33%) | 2/3 (67%) |
| entity_attribute | 3/3 (100%) | 3/3 (100%) |
| entity_relation | 3/3 (100%) | 3/3 (100%) |
| entity_type | 3/3 (100%) | 3/3 (100%) |
| page_rank_v2 | 2/3 (67%) | 0/3 (0%) |
| relation_between | 3/3 (100%) | 3/3 (100%) |
| relation_path | 3/3 (100%) | 3/3 (100%) |
| summary | 3/3 (100%) | 1/3 (33%) |
| tree_check | 3/3 (100%) | 0/3 (0%) |
| tree_lca | 2/3 (67%) | 1/3 (33%) |

## 3. 详细评测结果


### 3.1 BipartiteMatching

#### 样本 1

**预期输出**: `<<<{'max_matching': 0}>>>`

**graph_rl** ❌ (不匹配, 90.2s)
```
首先，用户的问题是：“假设该图是二分图结构，使用匈牙利算法计算最大匹配数。”

我需要理解图网络的结构。根据提供的信息，图是直接的（directed: true），但用户假设它是二分图结构。二分图（bipartite graph）是一种图，其节点集可以分为两个不相交的子集，使得图中所有边都从一个子集连接到另一个子集。

在给定的图中，节点有标签（labels）：
- 一些节点有标签 ["Entity", "DocGraph"]，表示它们是实体（Entity）类型。
- 其他节点有标签 ["Class"]，表示它们是类（Class）类型。

具体来说：
- 节点1到12：有标签 ["Entity", "DocGraph"]，例如 "场景"、"领导授权下属为其代理申请和报销" 等。
- 节点3到35：有标签 ["Class"]，例如 "iuap:mdd:权限profile表"、"YcmdApplicationParam" 等。

在二分图中，通常节点被分为两个部分：左部和右部。这里，可能的二分图划分是：
- 一部分是实体节点（Entity 类型）
- 另一部分是类节点（Class 类型）
...
```

**graph_sft** ❌ (不匹配, 0.5s)
```
<<<{19}>>>
```

#### 样本 2

**预期输出**: `<<<{'max_matching': 0}>>>`

**graph_rl** ❌ (不匹配, 17.8s)
```
首先，用户的问题是：“假设该图是二分图结构，使用匈牙利算法计算最大匹配数。”

我需要理解图网络的结构。根据提供的信息，图网络是：

- 有35个节点（id从1到35）
- 节点有标签（labels）和名称（name）
- 边（edges）是列表，每个边表示一个关系：所有边都从“我的单据”节点出发，指向其他节点，关系是“HAS_PROPERTY”

具体来说：
- 节点1：id=1，标签=["Class"]，名称="我的单据"，属性{"name": "我的单据"}
- 节点2到节点35：每个节点都有标签["Property", "DatatypeProperty"]，名称是各种属性名，如“期初标志”、“bustype”等。

边（edges）列表：
- 每条边都是["我的单据", "HAS_PROPERTY", "属性名"]，例如：["我的单据", "HAS_PROPERTY", "期初标志"]
- 所以，图是一个有向图，从节点“我的单据”（节点1）指向所有其他节点（节点2到节点35），关系是“HAS_PROPERTY”。

关键点：用户说“假设该图是二分图结构”。二分图（bipart...
```

**graph_sft** ❌ (不匹配, 0.3s)
```
<<<{35}>>>
```

#### 样本 3

**预期输出**: `<<<{'max_matching': 0}>>>`

**graph_rl** ❌ (不匹配, 22.3s)
```
首先，用户的问题是：“假设该图是二分图结构，使用匈牙利算法计算最大匹配数。”

我需要理解图网络的结构。根据提供的信息，图网络是二分图吗？规则中说“假设该图是二分图结构”，所以我应该基于这个假设来处理。

回顾图网络的描述：
- 它是一个有向图（directed: true）。
- 节点有标签：一些节点有标签 "Class"，其他有标签 "Property" 和 "DatatypeProperty"。
- 具体来说，节点1是 "Class" 标签，名称是 "申请单补贴明细"。
- 节点2到35都是 "Property" 和 "DatatypeProperty" 标签，名称是各种属性，如 "开始时间"、"结束时间" 等。

边（edges）是：所有边都从节点1（"申请单补贴明细"）指向其他节点，关系是 "HAS_PROPERTY"。例如：
- ["申请单补贴明细", "HAS_PROPERTY", "开始时间"]
- 类似地，所有边都以 "申请单补贴明细" 为起点，连接到其他属性节点。

所以，这个图实际上是一个星型图（star graph），其中中心节点是 "申请单补贴明细"，所有其他...
```

**graph_sft** ❌ (不匹配, 0.4s)
```
<<<{17}>>>
```


### 3.2 COMMON_NEIGHBOR

#### 样本 1

**预期输出**: `<<<{'报销'}>>>`

**graph_rl** ✅ (精确匹配, 37.4s)
```
首先，我需要理解用户的问题："报销单费用分摊特征表"和"预算控制"共同关联的业务对象有哪些？

根据规则，我必须先分析图网络的节点和关系，然后确定问题的类型，最后列出用于回答问题的信息。

回顾图网络的结构：
- 节点（nodes）有35个，每个节点有id、labels、name和properties。
- 边（edges）描述了节点之间的关系。

关键点是：用户问的是"报销单费用分摊特征表"和"预算控制"共同关联的业务对象有哪些？

我需要找出哪些业务对象同时与"报销单费用分摊特征表"和"预算控制"相关联。

首先，明确节点的名称：
- "报销单费用分摊特征表" 对应的节点：在nodes列表中，id=24，name="报销单费用分摊特征表"
- "预算控制" 对应的节点：在nodes列表中，id=33，name="预算控制"

现在，问题是要找共同关联的业务对象。这里的"关联"可能指的是图中通过边连接的节点。

在图网络中，边表示关系。我需要找出所有与"报销单费用分摊特征表"相关的节点，以及所有与"预算控制"相关的节点，然后找出它们的交集（共同关联的业务对象）。

但问题说"共同关联...
```

**graph_sft** ✅ (精确匹配, 0.4s)
```
<<<{'报销'}>>>
```

#### 样本 2

**预期输出**: `<<<{'报销'}>>>`

**graph_rl** ✅ (精确匹配, 19.1s)
```
首先，我需要理解用户的问题："YtsTaskLock'和'YtsContext'共同关联的业务对象有哪些？"

根据规则，我必须先分析图网络的节点和关系，然后确定问题的类型，最后列出用于回答问题的信息。

图网络的描述是：
- 节点（nodes）：有35个节点，每个节点有id、labels、name和properties。
- 边（edges）：有49条边，表示节点之间的关系。

关键点是：用户问的是"YtsTaskLock"和"YtsContext"共同关联的业务对象有哪些？

我需要找出YtsTaskLock和YtsContext都关联的业务对象。这里的"关联"可能指的是图中定义的关系。

先查看图网络中YtsTaskLock和YtsContext的节点信息：

- 节点22：id=12, labels=["Class"], name="YtsTaskLock", properties={"name": "YtsTaskLock"}
- 节点13：id=13, labels=["Class"], name="YtsContext", properties={"name": "Yts...
```

**graph_sft** ✅ (精确匹配, 0.3s)
```
<<<{'报销'}>>>
```

#### 样本 3

**预期输出**: `<<<set()>>>`

**graph_rl** ✅ (空集匹配, 51.2s)
```
首先，我需要理解用户的问题："修改日期"和"YcmdSession"共同关联的业务对象有哪些？

根据规则，我必须先分析图网络的结构，然后确定问题的类型，最后列出相关信息。

回顾图网络的描述：
- 这是一个有向图（directed: true）。
- 节点（nodes）有ID、标签、名称和属性。
- 边（edges）表示节点之间的关系。

关键点：
- 节点有名称，例如"修改日期"、"YcmdSession"等。
- 边表示关系，如"HAS_PROPERTY"、"EQUIVALENT_TO"、"属于"、"MENTIONS"等。

用户问题："修改日期"和"YcmdSession"共同关联的业务对象有哪些？

我需要找出哪些业务对象同时与"修改日期"和"YcmdSession"相关联。

首先，明确"业务对象"是什么。在图网络中，业务对象可能指的是图中的节点，特别是那些表示实体或类的节点。从图中看，节点有不同标签，如"Class"、"Property"、"Entity"等。

具体分析：
- "修改日期"：这是一个属性节点（Property节点），ID为11（从nodes列表中：id:...
```

**graph_sft** ✅ (空集匹配, 0.7s)
```
<<<{}>>>
```


### 3.3 EDGE

#### 样本 1

**预期输出**: `<<<{True}>>>`

**graph_rl** ✅ (精确匹配, 9.8s)
```
首先，我需要理解用户的问题："GovernmentBody'和'Government'之间是否存在直接的业务关联？"

根据规则，我必须先分析图网络的节点和关系。图网络的描述是：

- 节点：有23个节点，每个节点有id、labels、name和properties。
- 边：有22条边，所有边都是"SUB_CLASS_OF"关系，连接到"Government"节点。

具体看节点：
- 节点1：id=1, name="GovernmentAppointee", labels=["Class", "CoreClass"]
- 节点2：id=2, name="Government", labels=["Class", "CoreClass"]
- ... 一直到节点23：id=23, name="GovernmentBody", labels=["Class", "CoreClass"]

边列表：
- 边是：["GovernmentAppointee", "SUB_CLASS_OF", "Government"]
- ["DevolvedGovernment", "SUB_CLASS_...
```

**graph_sft** ✅ (精确匹配, 0.3s)
```
<<<{True}>>>
```

#### 样本 2

**预期输出**: `<<<{True}>>>`

**graph_rl** ✅ (精确匹配, 12.4s)
```
首先，我需要理解用户的问题："事项申请单差旅标准"和"补贴地点名称"之间是否存在直接的业务关联？

根据规则，我必须先分析图网络的节点和关系，然后确定问题的类型，最后列出用于回答问题的信息。

回顾图网络的结构：
- 节点（nodes）有35个，包括各种实体、类、属性等。
- 边（edges）描述了节点之间的关系。

关键点是：用户问的是"事项申请单差旅标准"和"补贴地点名称"之间是否存在直接的业务关联。

我需要检查图网络中是否有直接的边（关系）连接这两个节点。

先找到这两个节点的ID或名称：
- "事项申请单差旅标准"：在节点列表中，id=17，名称是"事项申请单差旅标准"，标签是["Class"]。
- "补贴地点名称"：在节点列表中，id=24，名称是"补贴地点名称"，标签是["Property", "DatatypeProperty"]。

现在，查看所有边（edges）来找出是否有直接的边连接这两个节点。

图网络的edges列表是：
[
  ["预算校验", "关联", "申请"],
  ["预算控制", "包含", "预算校验"],
  ["预算校验", "关联", ...
```

**graph_sft** ✅ (精确匹配, 0.4s)
```
<<<{True}>>>
```

#### 样本 3

**预期输出**: `<<<{True}>>>`

**graph_rl** ✅ (精确匹配, 12.7s)
```
首先，我需要理解用户的问题："还款"和"借款"之间是否存在直接的业务关联？

根据规则，我必须先分析图网络的节点和关系，然后确定问题的类型，最后列出用于回答问题的信息。

回顾图网络的定义：
- 这是一个有向图（directed: true）。
- 节点（nodes）有ID、标签（labels）、名称（name）和属性（properties）。
- 边（edges）表示节点之间的关系。

关键点是：用户问的是"还款"和"借款"之间是否存在直接的业务关联。

我需要检查图中是否有直接的边（即关系）连接"还款"节点和"借款"节点。

先找到"还款"和"借款"节点在图中的信息。

从图网络数据中：
- 节点ID 17：{"id": 17, "labels": ["Entity", "DocGraph"], "name": "还款", "properties": {"name": "还款"}}
- 节点ID 1：{"id": 1, "labels": ["Entity", "DocGraph"], "name": "借款", "properties": {"name": "借款"}}

现在，...
```

**graph_sft** ✅ (精确匹配, 0.3s)
```
<<<{True}>>>
```


### 3.4 MST

#### 样本 1

**预期输出**: `<<<{'mst_edge_count': 34}>>>`

**graph_rl** ✅ (数值匹配, 23.6s)
```
首先，用户的问题是：“如果要用最少的关联关系连接所有业务对象，需要多少条边？（最小生成树）”

我需要理解这个图网络。图网络是给定的JSON结构，描述了节点和边。

关键点是：用户问的是“最小生成树”（Minimum Spanning Tree, MST）的边数。最小生成树是图中连接所有节点的边的最小数量，不形成环。

规则4提到：需要遍历的情况，最大遍历深度限制为3次，最大路径数量限制最多为5条的查询。但这里不是遍历，而是求最小生成树的边数。

最小生成树的边数取决于图的连通性。如果图是连通的，那么最小生成树的边数就是节点数减一（n-1）。如果图不连通，那么可能需要多个生成树，但用户说“连接所有业务对象”，所以假设图是连通的。

先检查图是否连通。

图网络有35个节点（从metadata中：num_nodes: 35）。

边有35条（num_edges: 35）。

但图是否连通？我需要分析节点之间的连接。

从边列表中，我可以看到：

- 节点"控制属性"（id=2）是中心节点，它连接到很多节点。

具体看边：

- "控制属性" 有多个出边：DOMAIN_RELATED 到 ...
```

**graph_sft** ❌ (不匹配, 0.4s)
```
<<<{35}>>>
```

#### 样本 2

**预期输出**: `<<<{'mst_edge_count': 34}>>>`

**graph_rl** ✅ (数值匹配, 31.9s)
```
首先，用户的问题是：“如果要用最少的关联关系连接所有业务对象，需要多少条边？（最小生成树）”

我需要理解这个图网络。图网络是描述业务对象之间的关系。节点代表业务对象，边代表它们之间的关系。

从图网络的描述中：

- 节点（nodes）有ID、标签、名称和属性。

- 边（edges）是关系，如“HAS_PROPERTY”、“EQUIVALENT_TO”、“关联”等。

用户问的是“用最少的关联关系连接所有业务对象”，这听起来像是在问最小生成树（Minimum Spanning Tree, MST）的边数。

在图论中，最小生成树是连接所有节点的树结构，没有环，边数等于节点数减一。

但这里的关键是：图网络是给定的，我需要找出这个图的最小生成树的边数。

规则4提到：需要遍历的情况，最大遍历深度限制为3次，最大路径数量限制最多为5条的查询。但这里不是遍历，是求最小生成树。

最小生成树的边数是固定的：对于一个连通图，MST的边数 = 节点数 - 1。

首先，我需要确认图是否连通。

图网络的元数据：num_nodes: 35, num_edges: 44

节点数是35个。

如果...
```

**graph_sft** ❌ (不匹配, 0.4s)
```
<<<{19}>>>
```

#### 样本 3

**预期输出**: `<<<{'mst_edge_count': 34}>>>`

**graph_rl** ✅ (数值匹配, 31.4s)
```
首先，用户的问题是：“如果要用最少的关联关系连接所有业务对象，需要多少条边？（最小生成树）”

我需要理解这个图网络。图网络是给定的JSON结构，描述了节点和边。

关键点是：用户问的是“最小生成树”（Minimum Spanning Tree, MST）的边数。最小生成树是图中连接所有节点的边的最小集合，没有环，并且连接所有节点。

规则中提到：如果用户问题没有说明以下内容，请按照默认保留两位小数。返回路径或节点的权重或深度，均为1。阻尼因子等参数使用常见参数如0.85。需要遍历的情况，最大遍历深度限制为3次，最大路径数量限制最多为5条的查询。

但这里的问题是关于最小生成树的边数，不是路径或节点的权重。最小生成树的边数是固定的，取决于图的连通性。

首先，我需要检查图是否是连通的。因为如果图不连通，就不存在最小生成树（MST）来连接所有节点；MST只适用于连通图。

从图网络描述中：

- 节点数量：metadata中num_nodes是35。

- 边数量：num_edges是45。

图是直接的（directed: true），但最小生成树通常用于无向图。问题中说“图网络”，并...
```

**graph_sft** ❌ (不匹配, 0.4s)
```
<<<{35}>>>
```


### 3.5 NEIGHBOR

#### 样本 1

**预期输出**: `<<<{'申请'}>>>`

**graph_rl** ✅ (精确匹配, 33.8s)
```
首先，我需要理解用户的问题："在费控系统中，'申请单特征表'直接关联的所有业务对象有哪些？"

根据规则，我必须先分析图网络的结构，然后确定问题的类型，最后列出相关信息。

图网络的描述是：
- 它是一个有向图（directed: true）
- 节点（nodes）有ID、标签、名称和属性
- 边（edges）表示节点之间的关系

关键点是：用户问的是“'申请单特征表'直接关联的所有业务对象有哪些？”

我需要找出“申请单特征表”节点直接关联的业务对象。这里的“直接关联”应该指的是图中与该节点有边连接的其他节点。

首先，找到“申请单特征表”节点在图中的信息。

从图网络的nodes列表中：
- id: 19, labels: ["Class"], name: "申请单特征表", properties: {"name": "申请单特征表"}

所以，节点ID 19是“申请单特征表”。

现在，我需要找出所有与这个节点有直接边（edges）连接的节点。因为图是直接的，所以边表示关系。

查看edges列表：
edges是列表 of lists，每个元素是 [起点, 关系, 终点]

例如...
```

**graph_sft** ✅ (包含匹配, 0.6s)
```
<<<{'申请', '申请变更单变更后特征表', '申请单子表特征表', '申请变更单特征表'}>>>
```

#### 样本 2

**预期输出**: `<<<{'控制属性'}>>>`

**graph_rl** ❌ (不匹配, 43.3s)
```
首先，我需要理解用户的问题："在费控系统中，'敏感词、提示词'直接关联的所有业务对象有哪些？"

根据规则，我必须先分析图网络的结构，然后确定问题的类型，最后列出相关信息。

图网络的描述是：
- 它是一个有向图（directed: true）。
- 节点（nodes）有ID、标签、名称和属性。
- 边（edges）表示节点之间的关系。

关键点是：用户问的是“'敏感词、提示词'直接关联的所有业务对象有哪些？”

在图网络中：
- 节点有名称，例如 "敏感词、提示词" 是一个节点（id: 14）。
- 边表示关系，例如 "RELATED_TO"、"MENTIONS"、"DOMAIN_RELATED" 等。

问题说“直接关联的所有业务对象”。这里的“直接关联”应该指的是从“敏感词、提示词”节点出发，通过边连接的其他节点。

具体看图网络的edges部分：
- edges 列出了一些关系三元组。

我需要找到所有以 "敏感词、提示词" 为起点的边，因为问题问的是“敏感词、提示词”直接关联的业务对象，所以应该是从 "敏感词、提示词" 出发的边所指向的节点。

在有向图中：
- 边的表示是 ...
```

**graph_sft** ✅ (精确匹配, 0.4s)
```
<<<{'控制属性'}>>>
```

#### 样本 3

**预期输出**: `<<<{'借款'}>>>`

**graph_rl** ✅ (精确匹配, 39.1s)
```
首先，我需要理解用户的问题："在费控系统中，'借款单审批业务阶段'直接关联的所有业务对象有哪些？"

根据规则，我必须先分析图网络的结构，然后确定问题的类型，最后列出相关信息。

回顾图网络的描述：
- 这是一个有向图（directed: true）。
- 节点（nodes）有ID、标签（labels）、名称（name）和属性（properties）。
- 边（edges）表示节点之间的关系。

关键点是：用户问的是“'借款单审批业务阶段'直接关联的所有业务对象有哪些？”

我需要找出：
- 节点“借款单审批业务阶段”（在图中，ID为19，名称是"借款单审批业务阶段"）。
- 它直接关联的业务对象。这里的“直接关联”应该指的是图中与该节点有边连接的其他节点。

在图网络中，边（edges）表示关系。所以，“直接关联”可能意味着从该节点出发的出边（outgoing edges）指向的节点。

问题说“直接关联的所有业务对象”，在上下文中，“业务对象”可能指的是图中的节点，特别是那些表示具体业务实体的节点。

先确认节点“借款单审批业务阶段”的ID和名称：
- 查看nodes列表：id 1...
```

**graph_sft** ✅ (精确匹配, 0.3s)
```
<<<{'借款'}>>>
```


### 3.6 PREDECESSOR

#### 样本 1

**预期输出**: `<<<set()>>>`

**graph_rl** ✅ (空集匹配, 47.4s)
```
首先，用户的问题是：“哪些业务对象会流转到'待摊费用单审批当前审批人'？（即找出所有指向该节点的前驱节点）”

我需要理解图网络的结构。图网络是给定的JSON数据，描述了节点和边的关系。

关键点是：用户要找出所有指向“待摊费用单审批当前审批人”节点的前驱节点。也就是说，我需要找到所有节点，这些节点有边指向“待摊费用单审批当前审批人”。

在图网络中，节点有ID和名称。我需要找到节点名称为“待摊费用单审批当前审批人”的节点。

从图网络的nodes列表中查找：

- 节点ID 29的名称是“待摊费用单审批当前审批人”，属性是{"name": "待摊费用单审批当前审批人"}。

所以，目标节点是ID 29。

现在，我需要找出所有前驱节点，即所有节点，这些节点有边指向节点29。

在图网络中，边是用列表表示的。每条边是[起点, 关系, 终点]。

例如，边列表中有一条：["待摊费用单审批当前审批人", "RELATED_TO", "代理审批"]，但这是从“待摊费用单审批当前审批人”指向“代理审批”，所以这是后继节点，不是前驱。

前驱节点是那些有边指向“待摊费用单审批当前审批人”的节点。...
```

**graph_sft** ❌ (不匹配, 0.7s)
```
<<<{'代理审批'}>>>
```

#### 样本 2

**预期输出**: `<<<{'期初数据方案'}>>>`

**graph_rl** ✅ (精确匹配, 28.9s)
```
首先，用户的问题是：“哪些业务对象会流转到'核算和结算系统'？（即找出所有指向该节点的前驱节点）”

我需要理解这个图网络。图网络是给定的JSON结构，其中包含节点和边。

关键点是：用户问的是“哪些业务对象会流转到'核算和结算系统'？”，并指定“即找出所有指向该节点的前驱节点”。所以，这里“前驱节点”指的是在图中指向“核算和结算系统”节点的节点。也就是说，我需要找到所有节点，这些节点有边指向“核算和结算系统”。

在图网络中，边是表示关系的。图是定向的（directed: true），所以边有方向。

具体来说，问题说“指向该节点的前驱节点”，意思是“前驱节点”是那些有边指向“核算和结算系统”的节点。在图论中，前驱节点（predecessor）通常指在有向图中指向某个节点的节点。

所以，我需要找到所有节点，这些节点有边以“核算和结算系统”为终点。

先确认“核算和结算系统”节点的ID和名称。

从图网络的节点列表中：

- id: 20, "name": "核算和结算系统", "labels": ["Entity", "DocGraph"]

所以，节点名称是“核算和结算系统”。
...
```

**graph_sft** ✅ (精确匹配, 0.4s)
```
<<<{'期初数据方案'}>>>
```

#### 样本 3

**预期输出**: `<<<set()>>>`

**graph_rl** ✅ (空集匹配, 25.6s)
```
首先，用户的问题是：“哪些业务对象会流转到'AgreementCorporation-Banking'？（即找出所有指向该节点的前驱节点）”

我需要理解图网络的结构。图网络是直接的（directed: true），节点有ID、标签、名称和属性。边是关系，表示从一个节点到另一个节点的连接。

关键点是：用户问的是“哪些业务对象会流转到'AgreementCorporation-Banking'”，意思是找出所有指向该节点的前驱节点。在图中，前驱节点就是那些有边指向'AgreementCorporation-Banking'的节点。

从图网络描述中，我看到节点列表和边列表。

先找到'AgreementCorporation-Banking'节点。在节点列表中，ID 9 的节点是：{"id": 9, "labels": ["Class", "CoreClass"], "name": "AgreementCorporation-Banking", "properties": {"name": "AgreementCorporation-Banking"}}

所以，节点名称是 "Agre...
```

**graph_sft** ❌ (不匹配, 0.4s)
```
<<<{'Corporation'}>>>
```


### 3.7 SHORTEST_PATH

#### 样本 1

**预期输出**: `<<<set()>>>`

**graph_rl** ✅ (精确匹配, 63.2s)
```
首先，我需要理解用户的问题："从'费用预提处理业务数据特征表'到'票据采集'的最短业务流转路径经过哪些节点？"

根据规则，我需要分析图网络，找出从节点"费用预提处理业务数据特征表"到节点"票据采集"的最短路径，并列出路径上经过的节点。

回顾图网络的结构：
- 节点有ID、标签、名称和属性。
- 边（edges）表示节点之间的关系。

从图网络描述中：
- 节点列表（nodes）有35个节点，每个节点有名称。
- 边（edges）列出了所有关系。

具体到问题：
- 起点：'费用预提处理业务数据特征表'
- 终点：'票据采集'

我需要找到从"费用预提处理业务数据特征表"到"票据采集"的最短路径。路径是节点序列，经过的节点就是路径上的节点。

首先，我应该确认这些节点在图网络中的ID和关系。

从nodes列表中：
- 节点ID 31：名称是"费用预提处理业务数据特征表"（properties: {"name": "费用预提处理业务数据特征表"}）
- 节点ID 20：名称是"票据采集"（properties: {"name": "票据采集"}）

在edges列表中，边是用名称表示...
```

**graph_sft** ❌ (不匹配, 0.5s)
```
<<<{'费用预提处理业务数据特征表', '控制属性', '票据采集'}>>>
```

#### 样本 2

**预期输出**: `<<<{'账单报销规则', 'iuap:mdd:参照注册的用户级配置表', '代理人设置', '场景', '部门代理'}>>>`

**graph_rl** ❌ (不匹配, 34.4s)
```
首先，我需要理解用户的问题："从'账单报销规则'到'部门代理'的最短业务流转路径经过哪些节点？"

根据规则，我需要分析图网络，找出从节点"账单报销规则"到节点"部门代理"的最短路径，并列出路径上经过的节点。

回顾图网络的结构：
- 节点有ID、标签、名称和属性。
- 边有关系（如"包含"、"用于"、"MENTIONS"、"DOMAIN_RELATED"、"RELATED_TO"等）。

关键点：
- 路径是业务流转路径，所以应该关注有业务意义的关系，比如"RELATED_TO"或"DOMAIN_RELATED"等。
- 问题指定了"最短业务流转路径"，所以需要找最短的路径（节点数最少）。
- 规则4提到：需要遍历的情况，最大遍历深度限制为3次，最大路径数量限制最多为5条。但这里用户问的是最短路径，所以可能只有一条最短路径，或者我需要找到所有最短路径（但规则说最大路径数量5条，不过最短路径通常唯一或少数）。

首先，我需要找到节点"账单报销规则"和节点"部门代理"在图中的ID。

从图网络描述中：
- 节点30的名称是"账单报销规则"，ID为30。
- 节点17的名称是"部门代理"，...
```

**graph_sft** ❌ (不匹配, 0.7s)
```
<<<{'账单报销规则', 'iuap:mdd:参照注册的用户级配置表', '人员代理', '部门代理'}>>>
```

#### 样本 3

**预期输出**: `<<<{'YcmdSession', '控制属性', '票据采集'}>>>`

**graph_rl** ❌ (不匹配, 48.9s)
```
首先，我需要理解用户的问题："从'YcmdSession'到'票据采集'的最短业务流转路径经过哪些节点？"

根据规则，我需要分析图网络，找出从节点 "YcmdSession" 到节点 "票据采集" 的最短路径，并列出路径上经过的节点。

回顾图网络的结构：
- 节点有ID、标签、名称和属性。
- 边（edges）表示节点之间的关系。

从图网络描述中：
- 节点列表：每个节点有名称，例如 "YcmdSession" 是一个节点（id=5）。
- 边列表：描述了节点之间的关系。

具体看节点：
- "YcmdSession"：在节点列表中，id=5，标签是["Class"]，名称是"YcmdSession"。
- "票据采集"：在节点列表中，id=20，标签是["Entity", "DocGraph"]，名称是"票据采集"。

现在，我需要找到从 "YcmdSession" 到 "票据采集" 的最短路径。

首先，检查图中是否有直接的边（即最短路径长度为1）。

查看边列表（edges）：
- 边是列表形式，例如：["待摊费用单子实体", "RELATED_TO", "控制属性"]，表...
```

**graph_sft** ✅ (精确匹配, 0.5s)
```
<<<{'YcmdSession', '控制属性', '票据采集'}>>>
```


### 3.8 cycle_detection

#### 样本 1

**预期输出**: `<<<{True}>>>`

**graph_rl** ❌ (不匹配, 83.0s)
```
首先，用户的问题是：“该业务流程图中是否存在循环依赖或环路？”

我需要理解图网络的结构。图网络是给定的JSON，描述了节点和边的关系。图是定向的（directed: true）。

关键点是：问题问的是“该业务流程图中是否存在循环依赖或环路？”。这里的“业务流程图”可能指的是图网络中的某个特定节点或子图。在图中，节点ID 27的名称是“业务流程图”，所以问题可能是在问这个“业务流程图”节点是否涉及循环依赖或环路。

但问题说“该业务流程图中”，可能指的是整个图网络，或者更具体地，指代业务流程图节点本身。我需要仔细分析。

回顾规则：我需要先理解图网络各个节点的内涵和关系。

从图网络描述中：

- 节点有ID、标签、名称和属性。

- 边是关系，表示节点之间的连接。

问题具体是“是否存在循环依赖或环路”。在图论中，环路（cycle）是指从一个节点出发，经过一系列边，最终又回到该节点的路径。循环依赖可能指的是在流程中存在循环引用，导致无法完成。

由于图是定向的，我需要检查图中是否存在环路（cycle）。

规则4提到：如果用户问题没有说明以下内容，请按照默认保留两位小数。返回路径或...
```

**graph_sft** ❌ (不匹配, 0.5s)
```
<<<{False}>>>
```

#### 样本 2

**预期输出**: `<<<{False}>>>`

**graph_rl** ✅ (精确匹配, 13.4s)
```
首先，用户的问题是：“该业务流程图中是否存在循环依赖或环路？”

我需要理解图网络的结构。根据提供的图网络信息：

- 这是一个有向图（directed: true）。
- 节点（nodes）有35个，每个节点都有ID、标签（labels）、名称（name）和属性（properties）。
- 边（edges）有34条，所有边都是“SUB_CLASS_OF”关系。

关键点：所有边都是从一个节点指向另一个节点，表示“SUB_CLASS_OF”关系。例如，"AccountingFramework" 是 "Account" 的子类。

现在，问题问的是“是否存在循环依赖或环路”。在图论中，环路（cycle）是指从一个节点出发，经过一系列边后，又回到同一个节点的路径。

由于这个图是层次结构（所有边都是子类关系），它应该是一个有向无环图（DAG），因为子类关系通常是树状或有向无环的。在类层次结构中，循环依赖是不常见的，因为子类不能是自己的超类。

让我检查图的结构：

- 所有边都以“SUB_CLASS_OF”关系连接节点。
- 具体来说，每个边的格式是：[源节点名称, "SUB_CLASS...
```

**graph_sft** ✅ (精确匹配, 0.3s)
```
<<<{False}>>>
```

#### 样本 3

**预期输出**: `<<<{True}>>>`

**graph_rl** ✅ (精确匹配, 48.2s)
```
首先，用户的问题是：“该业务流程图中是否存在循环依赖或环路？”

我需要分析这个图网络，看看是否存在循环依赖或环路。循环依赖或环路在图论中指的是图中存在一个或多个环，即从一个节点出发，通过一系列边，最终能回到起点的路径。

根据提供的图网络信息：
- 图是定向的（directed: true）
- 节点有ID、标签、名称和属性
- 边有关系类型（如EQUIVALENT_TO, 关联, RELATED_TO, MENTIONS, DOMAIN_RELATED等）

关键点：图是定向的，所以环路（cycle）是指从一个节点开始，经过一条或多条有向边，最终回到同一个节点的路径。

我需要检查图中是否存在任何环路。

回顾图的边列表：
- 边是成对的，格式是["起点", "关系", "终点"]
- 例如：["申请", "EQUIVALENT_TO", "事项申请单子表"] 表示从“申请”节点到“事项申请单子表”节点，关系是EQUIVALENT_TO

为了检查环路，我需要遍历图，看是否有路径能从一个节点回到自己。

由于图是小的（35个节点，55条边），我可以手动分析或逻辑推断。

先列出所有...
```

**graph_sft** ❌ (不匹配, 0.4s)
```
<<<{False}>>>
```


### 3.9 description

#### 样本 1

**预期输出**: `<<<{"entity_type": "Class", "entity_name": "报销单代扣税孙表", "properties": {"name": "报销单代扣税孙表"}, "relation_type_counts": {}, "neighbor_type_counts": {}}>>>`

**graph_rl** ❌ (不匹配, 56.3s)
```
首先，我需要理解用户的问题："请描述业务对象'报销单代扣税孙表'的详细信息，包括类型、属性和关联关系统计。"

根据规则，我必须先分析图网络的各个节点和关系，然后确定问题的类型，最后列出用于回答问题的信息。

回顾图网络的结构：
- 节点（nodes）：有35个节点，每个节点有id、labels、name和properties。
- 边（edges）：有44条边，表示节点之间的关系。
- 图是定向的（directed: true）。

关键点是用户要描述业务对象"报销单代扣税孙表"的详细信息，包括：
1. 类型（type）
2. 属性（properties）
3. 关联关系统计（association relationship statistics）

我需要从图网络中提取相关信息。

先找到节点"报销单代扣税孙表"：
- 在nodes列表中，id=1的节点是：{"id": 1, "labels": ["Class"], "name": "报销单代扣税孙表", "properties": {"name": "报销单代扣税孙表"}}
- 所以，这个节点的类型（label）是["Class...
```

**graph_sft** ❌ (不匹配, 2.0s)
```
<<<{'类型': 'Class', '属性': ['本币代扣税税额', '本币税基', '修改时间', '代扣税税目(来源BD.TAXRATE.TAXRATEVO)', '全局分布式事务ID', '逻辑删除标记(0-有效，1-删除)', '修改人ID', '时间戳', '与代扣税孙表关联的报销明细表字段(报销明细也有这个字段)', '序号', '税基', '创建时间', '创建人ID', '报销', '分布式事务的状态(0解冻，1冻结)', '租户', '租户ID', '代扣税税额', '行号', '税率'], '关联关系统计': {'EQUIVALENT_TO': 1, '关联': 1, 'MENTIONS': 1, 'RELATED_TO': 1}}>>>
```

#### 样本 2

**预期输出**: `<<<{"entity_type": "Class", "entity_name": "OrganizationIdentificationScheme", "properties": {"name": "OrganizationIdentificationScheme"}, "relation_type_counts": {}, "neighbor_type_counts": {}}>>>`

**graph_rl** ✅ (实体匹配, 40.3s)
```
首先，用户的问题是：“请描述业务对象'OrganizationIdentificationScheme'的详细信息，包括类型、属性和关联关系统计。”

我需要根据给定的图网络信息来回答这个问题。图网络的结构是：有节点（nodes）和边（edges），表示关系。

关键点是：用户指定了业务对象“OrganizationIdentificationScheme”。在图网络中，这个对象是一个节点。

让我先找到节点“OrganizationIdentificationScheme”的信息。

从图网络的nodes列表中：

- id: 16, labels: ["Class", "CoreClass"], name: "OrganizationIdentificationScheme", properties: {"name": "OrganizationIdentificationScheme"}

所以，节点“OrganizationIdentificationScheme”的属性是：name: "OrganizationIdentificationScheme"。

现在，问题要求描述...
```

**graph_sft** ✅ (名称匹配, 0.6s)
```
<<<{'OrganizationIdentificationScheme', 'Class', 'CoreClass', 'SUB_CLASS_OF', 'Organization'}>>>
```

#### 样本 3

**预期输出**: `<<<{"entity_type": "Class", "entity_name": "公务卡流水特征扩展表", "properties": {"name": "公务卡流水特征扩展表"}, "relation_type_counts": {}, "neighbor_type_counts": {}}>>>`

**graph_rl** ❌ (不匹配, 38.0s)
```
首先，我需要理解用户的问题："请描述业务对象'公务卡流水特征扩展表'的详细信息，包括类型、属性和关联关系统计。"

根据规则，我必须先分析图网络的各个节点和关系，然后确定问题的类型，最后列出用于回答问题的信息。

回顾图网络的结构：
- 节点（nodes）：有35个节点，每个节点有id、labels、name和properties。
- 边（edges）：有35条边，表示节点之间的关系。

用户指定了业务对象是"公务卡流水特征扩展表"。我需要找到这个节点在图网络中的信息。

从图网络的nodes列表中搜索：
- id: 25, name: "公务卡流水特征扩展表", properties: {"name": "公务卡流水特征扩展表"}

所以，节点25就是"公务卡流水特征扩展表"。

现在，问题要求描述：
1. 类型（type）：在图网络中，节点的类型由labels字段表示。这个节点的labels是["Class"]，所以类型是"Class"。
2. 属性（attributes）：节点的properties中有一个"name"属性，值是"公务卡流水特征扩展表"。但问题说"属性"，可能指...
```

**graph_sft** ✅ (名称匹配, 0.5s)
```
<<<{'公务卡流水特征扩展表', 'RELATED_TO', '控制属性'}>>>
```


### 3.10 entity_attribute

#### 样本 1

**预期输出**: `<<<{'YcmdApplicationParam'}>>>`

**graph_rl** ✅ (精确匹配, 5.6s)
```
首先，我需要理解用户的问题："业务对象'YcmdApplicationParam'的名称属性值是什么？"

根据规则，我必须先分析图网络的各个节点和关系，然后确定问题的类型，最后列出用于回答问题的信息。

回顾图网络的描述：
- 图网络是直接的（directed: true）。
- 节点有id、labels、name和properties。
- 边（edges）表示节点之间的关系。

关键点是：用户问的是"业务对象'YcmdApplicationParam'的名称属性值是什么？"

我需要找到节点"YcmdApplicationParam"，并查看它的属性（properties）。

从图网络的节点列表中：
- 节点id 5：{"id": 5, "labels": ["Class"], "name": "YcmdApplicationParam", "properties": {"name": "YcmdApplicationParam"}}

这里，节点"YcmdApplicationParam"的properties属性是{"name": "YcmdApplicationParam...
```

**graph_sft** ✅ (精确匹配, 0.4s)
```
<<<{'YcmdApplicationParam'}>>>
```

#### 样本 2

**预期输出**: `<<<{'预算控制'}>>>`

**graph_rl** ✅ (精确匹配, 9.5s)
```
首先，用户的问题是：“业务对象'预算控制'的名称属性值是什么？”

我需要理解图网络的结构。图网络有节点和边。节点有id、labels、name和properties属性。

关键点是：用户问的是“业务对象'预算控制'的名称属性值”。这里的“业务对象”可能指的是图网络中的节点。

在图网络中，节点有名称（name属性）和属性（properties）。每个节点都有一个"name"字段，表示节点的名称。

查看图网络的描述：

- 节点列表中，有多个节点，每个节点有"name"属性。

例如：
- 节点32：{"id": 32, "labels": ["Entity", "DocGraph"], "name": "预算编制", "properties": {"name": "预算编制"}}
- 节点33：{"id": 33, "labels": ["Entity", "DocGraph"], "name": "预算控制", "properties": {"name": "预算控制"}}

具体地，节点33的name是"预算控制"，properties是{"name": "预算控制"}。

...
```

**graph_sft** ✅ (精确匹配, 0.5s)
```
<<<{'预算控制'}>>>
```

#### 样本 3

**预期输出**: `<<<{'EmployerIdentificationNumberingScheme'}>>>`

**graph_rl** ✅ (精确匹配, 11.1s)
```
首先，用户的问题是：“业务对象'EmployerIdentificationNumberingScheme'的名称属性值是什么？”

我需要理解图网络的结构。图网络描述了节点和边的关系。节点有id、labels、name和properties属性。每个节点的properties中有一个"name"属性，表示该节点的名称。

具体到问题：用户问的是“业务对象'EmployerIdentificationNumberingScheme'的名称属性值是什么？”

在图网络中，节点列表里有：

- id: 17, name: "EmployerIdentificationNumberingScheme", properties: {"name": "EmployerIdentificationNumberingScheme"}

所以，这个节点的名称属性值就是"EmployerIdentificationNumberingScheme"。

根据规则1：返回属性值、数量、名称、布尔值的回答。这里用户问的是名称属性值，所以应该返回一个属性值。

规则1说：将回答的值放在一个集合中，外部用<<<>...
```

**graph_sft** ✅ (精确匹配, 0.5s)
```
<<<{'EmployerIdentificationNumberingScheme'}>>>
```


### 3.11 entity_relation

#### 样本 1

**预期输出**: `<<<{'HAS_PROPERTY'}>>>`

**graph_rl** ✅ (精确匹配, 13.4s)
```
首先，用户的问题是：“业务事项通用规则适用单据”与“单据类型”之间是什么类型的业务关系？

我需要理解图网络的结构。根据提供的图网络信息：

- 节点有6个：
  - id 1: 标签 ["Class"]，名称 "业务事项通用规则适用单据"，属性 {"name": "业务事项通用规则适用单据"}
  - id 2: 标签 ["Property", "DatatypeProperty"]，名称 "交易类型"，属性 {"name": "交易类型"}
  - id 3: 标签 ["Property", "DatatypeProperty"]，名称 "单据类型"，属性 {"name": "单据类型"}
  - id 4: 标签 ["Property", "DatatypeProperty"]，名称 "主表主键"，属性 {"name": "主表主键"}
  - id 5: 标签 ["Property", "DatatypeProperty"]，名称 "时间戳"，属性 {"name": "时间戳"}
  - id 6: 标签 ["Property", "DatatypeProperty"]，名称 ...
```

**graph_sft** ✅ (精确匹配, 0.2s)
```
<<<{'HAS_PROPERTY'}>>>
```

#### 样本 2

**预期输出**: `<<<{'HAS_PROPERTY'}>>>`

**graph_rl** ✅ (精确匹配, 12.7s)
```
首先，我需要理解用户的问题："结算变更单单审批业务阶段"与"时间戳"之间是什么类型的业务关系？

根据提供的图网络信息，图网络是直接的（directed: true），有10个节点和9条边。节点和边的描述如下：

- 节点1：id=1，标签是["Class"]，名称是"结算变更单单审批业务阶段"，属性是{"name": "结算变更单单审批业务阶段"}
- 节点2：id=2，标签是["Property", "DatatypeProperty"]，名称是"状态标志位"，属性是{"name": "状态标志位"}
- 节点3：id=3，标签是["Property", "DatatypeProperty"]，名称是"流程实例"，属性是{"name": "流程实例"}
- 节点4：id=4，标签是["Property", "DatatypeProperty"]，名称是"时间戳"，属性是{"name": "时间戳"}
- 节点5：id=5，标签是["Property", "DatatypeProperty"]，名称是"业务阶段编码最后修改时间"，属性是{"name": "业务阶段编码最后修改时间"}...
```

**graph_sft** ✅ (精确匹配, 0.4s)
```
<<<{'HAS_PROPERTY'}>>>
```

#### 样本 3

**预期输出**: `<<<{'HAS_PROPERTY'}>>>`

**graph_rl** ✅ (精确匹配, 14.2s)
```
首先，我需要理解用户的问题："回写申请中间表"与"申请单据行id"之间是什么类型的业务关系？

根据提供的图网络信息，这个图网络是直接的（directed: true），节点是各种属性，边表示关系。具体来说，节点有ID、标签、名称和属性。

关键点是：用户问的是"回写申请中间表"和"申请单据行id"之间的业务关系类型。

在图网络中，边（edges）表示节点之间的关系。每个边是一个三元组：起点（source）- 关系（relation）- 终点（target）。

从图网络的edges列表中，我可以看到所有边。例如：

- ["回写申请中间表", "HAS_PROPERTY", "汇率日期"]

- ["回写申请中间表", "HAS_PROPERTY", "可报销次数"]

- ... 依此类推

具体到"申请单据行id"，我需要找到所有以"回写申请中间表"为起点，以"申请单据行id"为终点的边。

查看edges列表：

edges = [
  ["回写申请中间表", "HAS_PROPERTY", "汇率日期"],
  ["回写申请中间表", "HAS_PROPERTY", "可报...
```

**graph_sft** ✅ (精确匹配, 0.4s)
```
<<<{'HAS_PROPERTY'}>>>
```


### 3.12 entity_type

#### 样本 1

**预期输出**: `<<<{'Class'}>>>`

**graph_rl** ✅ (精确匹配, 22.2s)
```
首先，我需要理解用户的问题："预算编制子表特征表"在系统中属于什么类型的业务对象？

根据规则，我必须先分析图网络的各个节点和关系，然后确定问题的类型，最后列出用于回答问题的信息。

回顾图网络的描述：
- 节点有ID、标签、名称和属性。
- 边表示节点之间的关系。

关键点是：用户问的是"预算编制子表特征表"在系统中属于什么类型的业务对象。

我需要查看图网络中关于这个节点的信息。

从图网络的节点列表中，找到ID为17的节点：
- id: 17
- labels: ["Class"]
- name: "预算编制子表特征表"
- properties: {"name": "预算编制子表特征表"}

这里，标签（labels）是["Class"]，这表示这个节点被标记为"Class"类型。

在图网络中，节点的标签（labels）描述了节点的类别。例如：
- 一些节点有标签["Entity", "DocGraph"]，表示它们是实体和文档图的节点。
- 一些节点有标签["Class"]，表示它们是类（Class）节点。

具体到"预算编制子表特征表"节点：
- 它的标签是["Class...
```

**graph_sft** ✅ (精确匹配, 0.4s)
```
<<<{'Class'}>>>
```

#### 样本 2

**预期输出**: `<<<{'Entity'}>>>`

**graph_rl** ✅ (精确匹配, 13.7s)
```
首先，我需要理解用户的问题："调研单位数量"在系统中属于什么类型的业务对象？

根据规则，我必须先分析图网络的各个节点的内涵和关系，然后确定问题的类型，最后列出用于回答问题的信息。

回顾图网络的描述：
- 图网络是直接的（directed: true）。
- 节点（nodes）有ID、标签（labels）、名称（name）和属性（properties）。
- 边（edges）表示节点之间的关系。

关键点是：用户问的是“调研单位数量”在系统中属于什么类型的业务对象。

我需要查看图网络中“调研单位数量”节点的属性，特别是它的标签（labels）和类型。

从图网络的节点列表中查找“调研单位数量”：
- 节点ID 12：{"id": 12, "labels": ["Entity", "DocGraph"], "name": "调研单位数量", "properties": {"name": "调研单位数量"}}

这里，节点的标签（labels）是["Entity", "DocGraph"]。标签表示节点的类型。

在图网络中，标签（labels）定义了节点的类别。例如：
- "Enti...
```

**graph_sft** ✅ (部分匹配(50%), 0.6s)
```
<<<{'Entity', 'DocGraph'}>>>
```

#### 样本 3

**预期输出**: `<<<{'Entity'}>>>`

**graph_rl** ✅ (精确匹配, 16.5s)
```
首先，我需要理解用户的问题："代理时间维护"在系统中属于什么类型的业务对象？

根据规则，我必须先分析图网络的各个节点和关系，然后确定问题的类型，最后列出用于回答问题的信息。

回顾图网络的描述：
- 节点（nodes）有35个，每个节点有id、labels、name和properties。
- 边（edges）表示节点之间的关系。
- 图是定向的（directed: true）。

关键点是：用户问“代理时间维护”在系统中属于什么类型的业务对象？

我需要找出“代理时间维护”这个节点的类型。在图网络中，节点的类型通常由其labels字段表示。

查看图网络中的节点：
- 节点20的id是20，labels是["Entity", "DocGraph"]，name是"代理时间维护"，properties是{"name": "代理时间维护"}。

所以，“代理时间维护”这个节点的labels是["Entity", "DocGraph"]。

问题问的是“属于什么类型的业务对象”。在图网络中，业务对象的类型可能由节点的labels来定义。

labels字段表示节点的类型标签。这里，节点的l...
```

**graph_sft** ✅ (精确匹配, 0.4s)
```
<<<{'Entity'}>>>
```


### 3.13 page_rank_v2

#### 样本 1

**预期输出**: `<<<{'需求调研': 0.15}>>>`

**graph_rl** ❌ (不匹配, 111.0s)
```
首先，用户的问题是：“使用PageRank算法分析该业务网络，哪些业务对象最重要？返回前3个。”

我需要理解图网络的结构。图网络是给定的JSON数据，描述了节点和边。

关键点：
- 图是定向的（directed: true）。
- 节点有ID、标签、名称和属性。
- 边表示节点之间的关系。

用户要求使用PageRank算法来分析哪些业务对象最重要，并返回前3个。

PageRank算法是用于评估网络中节点重要性的算法，它基于节点的链接数量和链接质量。在PageRank中，重要性（PageRank值）高的节点被认为是更中心的节点。

根据规则：
- 返回属性值、数量、名称、布尔值的回答：但这里用户问的是PageRank分析，所以应该返回节点的PageRank值。
- 规则4提到：如果用户问题没有说明以下内容，请按照默认保留两位小数。返回路径或节点的权重或深度，均为1。阻尼因子等参数使用常见参数如0.85。需要遍历的情况，最大遍历深度限制为3次，最大路径数量限制最多为5条的查询。

对于PageRank，我需要计算每个节点的PageRank值。PageRank的计算涉及：
- 阻尼因...
```

**graph_sft** ❌ (不匹配, 0.6s)
```
<<<{'客户名称', '费控业务', 'YcmdApplicationParam'}>>>
```

#### 样本 2

**预期输出**: `<<<{'交易类型': 0.15}>>>`

**graph_rl** ✅ (包含目标节点, 92.8s)
```
首先，用户的问题是：“使用PageRank算法分析该业务网络，哪些业务对象最重要？返回前3个。”

我需要理解图网络的结构。图网络是给定的JSON数据，描述了节点和边。

关键点：
- 图是定向的（directed: true）。
- 节点有ID、标签、名称和属性。
- 边是关系，如“包含”、“引用”、“控制”等。

PageRank算法用于评估图中节点的重要性。PageRank基于节点的链接数量和链接质量。在PageRank中，重要性（PageRank值）高的节点表示更中心的节点。

规则说明：
- 返回前3个最重要的业务对象（节点）。
- 需要使用PageRank算法。
- 默认参数：阻尼因子为0.85（常见值），最大遍历深度限制为3次（但PageRank通常迭代计算，深度限制可能不直接适用；这里可能指在计算中考虑的路径深度）。
- 最大路径数量限制最多为5条（但PageRank是全局计算，不是路径查询）。

规则4说：“需要遍历的情况，最大遍历深度限制为3次，最大路径数量限制最多为5条的查询。” 但PageRank是迭代算法，不是简单路径查询。所以，我需要应用PageRank标准...
```

**graph_sft** ❌ (不匹配, 0.6s)
```
<<<{'稽核规则', 'iuap:mdd:表单字段规则脚本预制表', 'iuap:mdd:单据助手配置表'}>>>
```

#### 样本 3

**预期输出**: `<<<{'发票抬头规则': 0.15}>>>`

**graph_rl** ✅ (包含目标节点, 85.7s)
```
首先，用户的问题是：“使用PageRank算法分析该业务网络，哪些业务对象最重要？返回前3个。”

我需要理解图网络的结构。图网络是给定的JSON数据，其中：
- `directed`: true，表示有向图。
- `nodes`: 列出所有节点，每个节点有id、labels、name和properties。
- `edges`: 列出所有边，每条边是三元组（起点，关系，终点）。

关键点是：用户要求使用PageRank算法分析哪些业务对象最重要，并返回前3个。

PageRank算法是用于有向图中节点重要性的度量，它基于节点的链接数量和链接质量。在PageRank中，每个节点的权重（PageRank值）表示其重要性。

根据规则：
- 返回属性值、数量、名称、布尔值的回答：但这里用户问的是最重要的业务对象，所以应该返回节点名称。
- 规则3：返回节点，直接使用节点name的字符串，放入一个set中。

具体到这个问题：
- 我需要计算整个图的PageRank值。
- 然后，按PageRank值从高到低排序，取前3个节点。
- 规则4提到：如果用户问题没有说明以下内容，请按照默认保留两...
```

**graph_sft** ❌ (不匹配, 0.7s)
```
<<<{'iuap:mdd:表单字段规则脚本预制表', 'iuap:mdd:表单字段规则公式定义表', 'iuap:mdd:单据助手配置表'}>>>
```


### 3.14 relation_between

#### 样本 1

**预期输出**: `<<<{'集成'}>>>`

**graph_rl** ✅ (部分匹配(50%), 20.9s)
```
首先，我需要理解用户的问题："费控系统"和"第三方系统"之间的关联类型是什么？

根据提供的图网络信息，图网络是直接的（directed: true），有节点和边（edges）。

我应该查看图网络中是否有直接的边连接"费控系统"和"第三方系统"。

从图网络的edges列表中，我找到以下相关边：

- ["费控系统", "集成", "第三方系统"]：这表示费控系统集成到第三方系统。

- ["费控系统", "DOMAIN_RELATED", "第三方系统"]：这表示费控系统和第三方系统有DOMAIN_RELATED关系。

用户问的是"关联类型"，所以我需要确定这两个节点之间有哪些关系类型。

在edges列表中，有两条边涉及费控系统和第三方系统：

1. ["费控系统", "集成", "第三方系统"] — 这表示费控系统集成第三方系统。

2. ["费控系统", "DOMAIN_RELATED", "第三方系统"] — 这表示费控系统和第三方系统有DOMAIN_RELATED关系。

问题问的是"关联类型"，可能指的是直接的边类型。

在图网络中，边有类型（relation），比如...
```

**graph_sft** ✅ (精确匹配, 0.3s)
```
<<<{'集成'}>>>
```

#### 样本 2

**预期输出**: `<<<{'DOMAIN_RELATED'}>>>`

**graph_rl** ✅ (精确匹配, 18.5s)
```
首先，我需要理解用户的问题："期初数据方案"和"支出合同"之间的关联类型是什么？

根据提供的图网络信息，图网络是直接的（directed: true），有节点和边。节点有ID、标签、名称和属性。边表示节点之间的关系。

我需要找到"期初数据方案"和"支出合同"之间的关联类型。也就是说，我需要查看图中是否有直接的边连接这两个节点，并且确定边的类型（关系）。

让我从图网络的edges列表中查找相关边。

图网络的edges列表是：

[
  ["期初数据方案", "包含", "未摊销完成的摊销数据"],
  ["期初数据方案", "包含", "未履约完成的支出合同"],
  ["期初数据方案", "包含", "未冲销或还款的个人借款余额"],
  ["期初数据方案", "包含", "已支付但未收到发票的对公预付"],
  ["期初数据方案", "包含", "期初数据清单及处理方式"],
  ["费控系统", "依赖", "期初数据方案"],
  ["期初数据方案", "作为", "后续流程闭环的前置"],
  ["YcmdApplicationParam", "MENTIONS", "...
```

**graph_sft** ✅ (精确匹配, 0.5s)
```
<<<{'DOMAIN_RELATED'}>>>
```

#### 样本 3

**预期输出**: `<<<{'EQUIVALENT_TO'}>>>`

**graph_rl** ✅ (精确匹配, 23.0s)
```
首先，我需要理解用户的问题："报销'和'报销单子表特征表'之间的关联类型是什么？"

根据规则，我必须先分析图网络的节点和关系，然后确定问题的类型，最后列出用于回答问题的信息。

回顾图网络的描述：
- 图网络是定向的（directed: true）。
- 节点有ID、标签、名称和属性。
- 边（edges）表示节点之间的关系。

关键点是：用户问的是"报销"和"报销单子表特征表"之间的关联类型。

在图网络中，我需要找到这两个节点之间的关系。

先找出节点的ID和名称：
- 节点"报销"：在nodes列表中，id=21，名称是"报销"，标签是["Entity", "DocGraph"]。
- 节点"报销单子表特征表"：在nodes列表中，id=23，名称是"报销单子表特征表"，标签是["Class"]。

现在，查看边（edges）列表，看看这两个节点之间有什么关系。

边列表（edges）中：
- 有边表示"报销"和"报销单子表特征表"之间的关系。

具体看edges：
- ["报销单子表特征表", "RELATED_TO", "报销"] — 这条边表示"报销单子表特征表"与"报销...
```

**graph_sft** ✅ (精确匹配, 0.5s)
```
<<<{'EQUIVALENT_TO'}>>>
```


### 3.15 relation_path

#### 样本 1

**预期输出**: `<<<{frozenset([('结算变更单结算信息', 'HAS_PROPERTY', '付款账户类型')])}>>>`

**graph_rl** ✅ (集合匹配, 21.9s)
```
首先，我需要理解用户的问题："找出从'结算变更单结算信息'到'付款账户类型'的业务关系路径。"

根据规则，我需要分析图网络，找出从节点"结算变更单结算信息"到节点"付款账户类型"的路径。路径应该以"起点-关系-终点"三元组的形式表示。

回顾图网络的结构：
- 节点：有35个节点，每个节点有id、labels、name和properties。
- 边：有34条边，所有边都从"结算变更单结算信息"节点出发，关系是"HAS_PROPERTY"，连接到其他节点。

具体看节点：
- 起点节点：名称是"结算变更单结算信息"，对应id=1。
- 终点节点：名称是"付款账户类型"，对应id=3。

在边列表中：
- 所有边都以"结算变更单结算信息"作为起点，关系是"HAS_PROPERTY"，终点是各种属性节点。
- 例如：["结算变更单结算信息", "HAS_PROPERTY", "账户类型"]，["结算变更单结算信息", "HAS_PROPERTY", "付款账户类型"]，等等。

所以，从"结算变更单结算信息"到"付款账户类型"的路径非常直接：
- 起点：结算变更单结算信息
- 关系：HA...
```

**graph_sft** ✅ (集合匹配, 0.5s)
```
<<<{'结算变更单结算信息', 'HAS_PROPERTY', '付款账户类型'}>>>
```

#### 样本 2

**预期输出**: `<<<{frozenset([('稽核规则', 'HAS_PROPERTY', '创建时间')])}>>>`

**graph_rl** ✅ (集合匹配, 13.6s)
```
首先，我需要理解用户的问题："找出从'稽核规则'到'创建时间'的业务关系路径。"

根据规则，我需要分析图网络，找出从节点"稽核规则"到节点"创建时间"的路径。路径应该由"起点-关系-终点"三元组组成。

回顾图网络的结构：
- 节点有id、labels、name和properties。
- 边（edges）描述了节点之间的关系。

关键点：
- 起点是"稽核规则"。
- 终点是"创建时间"。
- 路径需要是业务关系路径。问题中指定了"业务关系路径"，所以我需要关注图中定义的边类型，特别是那些表示业务关系的边。

先找出图中所有与"稽核规则"和"创建时间"相关的节点和边。

从图网络描述中：
- 节点"稽核规则"的id是1（根据nodes列表：{"id": 1, "labels": ["Class"], "name": "稽核规则", ...}）
- 节点"创建时间"的id是28（根据nodes列表：{"id": 28, "labels": ["Property", "DatatypeProperty"], "name": "创建时间", ...}）

现在，查看边（edges）列表，...
```

**graph_sft** ✅ (集合匹配, 0.4s)
```
<<<{'稽核规则', 'HAS_PROPERTY', '创建时间'}>>>
```

#### 样本 3

**预期输出**: `<<<{frozenset([('YcmdSession', 'MENTIONS', '借款')])}>>>`

**graph_rl** ✅ (集合匹配, 30.3s)
```
首先，我需要理解用户的问题："找出从'YcmdSession'到'借款'的业务关系路径。"

根据规则，我需要分析图网络，找出从节点 "YcmdSession" 到节点 "借款" 的路径。路径应该以「起点-关系-终点」三元组的形式表示。

回顾图网络的结构：
- 节点有ID、标签、名称和属性。
- 边（edges）表示节点之间的关系。

从图网络描述中，我找到相关节点：
- "YcmdSession" 是一个节点，ID为14（因为节点列表中ID 14的name是"YcmdSession"）。
- "借款" 是一个节点，ID为6（节点列表中ID 6的name是"借款"）。

现在，我需要找出从 "YcmdSession" 到 "借款" 的路径。路径是节点之间的关系链。

查看edges列表：
- edges是列表，每个元素是三元组：[起点, 关系, 终点]
- 例如：["YcmdApplicationParam", "MENTIONS", "借款"] 表示 YcmdApplicationParam 节点通过 MENTIONS 关系连接到 借款 节点。

具体找从 "YcmdSession...
```

**graph_sft** ✅ (集合匹配, 0.5s)
```
<<<{'YcmdSession', 'MENTIONS', '借款'}>>>
```


### 3.16 summary

#### 样本 1

**预期输出**: `<<<{"node_count": 35, "edge_count": 45, "node_type_counts": {"Entity": 6, "Class": 29}, "relation_type_counts": {"配置": 2, "包含": 2, "引用": 1, "控制": 2, "使用": 1, "RELATED_TO": 29, "DOMAIN_RELATED": 7, "EQ...`

**graph_rl** ✅ (数值匹配, 54.0s)
```
首先，用户的问题是：“请统计该业务子图的基本信息：节点数、边数、各类型节点数量、各类型关系数量。”

我需要根据给定的图网络信息来回答这个问题。图网络的描述是JSON格式，其中包含节点和边的信息。

回顾规则：
- 我需要返回属性值、数量、名称、布尔值等，用特定的格式。
- 具体到这个问题，用户要求统计：节点数、边数、各类型节点数量、各类型关系数量。

从图网络的JSON中提取信息：
- 节点数：JSON中有一个"metadata"字段，其中"num_nodes"是35。所以节点数是35。
- 边数：同样在"metadata"中，"num_edges"是45。所以边数是45。

接下来，各类型节点数量：
- 节点有不同标签（labels）。在JSON中，每个节点有"labels"属性。
- 例如：
  - 节点1：labels = ["Entity", "DocGraph"]
  - 节点2：labels = ["Entity", "DocGraph"]
  - ... 以此类推。
- 我需要统计每个标签类型的节点数量。

查看所有节点的labels：
- 节点1到节点10（id 1到...
```

**graph_sft** ❌ (不匹配, 1.2s)
```
<<<{'node_num': 35, 'edge_num': 45, 'node_types': {'Entity': 5, 'Class': 30}, 'relation_types': {'配置': 3, '包含': 2, '引用': 1, '控制': 2, '使用': 1, 'RELATED_TO': 30, 'DOMAIN_RELATED': 5, 'EQUIVALENT_TO': 1}, 'relation_num': 34}>>>
```

#### 样本 2

**预期输出**: `<<<{"node_count": 35, "edge_count": 50, "node_type_counts": {"Entity": 17, "Class": 7, "Property": 11}, "relation_type_counts": {"属于": 1, "集成": 1, "MENTIONS": 21, "DOMAIN_RELATED": 16, "HAS_PROPERTY":...`

**graph_rl** ✅ (数值匹配, 84.0s)
```
首先，用户的问题是：“请统计该业务子图的基本信息：节点数、边数、各类型节点数量、各类型关系数量。”

我需要根据给定的图网络信息来回答这个问题。图网络的描述是JSON格式，我需要从中提取必要的信息。

回顾规则：
- 返回属性值、数量、名称、布尔值的回答：将值放在一个集合中，用<<<>>>括起来。
- 其他规则如返回路径、节点等不适用，因为这个问题是统计基本信息。

具体问题要求：
- 节点数
- 边数
- 各类型节点数量
- 各类型关系数量

从图网络的JSON中，我可以看到：
- "nodes" 列表：有35个节点（因为metadata中num_nodes: 35）
- "edges" 列表：有50条边（metadata中num_edges: 50）

所以：
- 节点数：35
- 边数：50

接下来，各类型节点数量。节点有不同标签（labels）：
- 从nodes列表中，每个节点有"labels"属性。
- 节点类型可以基于标签来分类。

列出所有节点的标签：
- 节点1：labels ["Entity", "DocGraph"]
- 节点2：labels ["Entity"...
```

**graph_sft** ✅ (数值匹配, 1.8s)
```
<<<{'node_count': 35, 'edge_count': 50, 'class_count': 10, 'property_count': 10, 'entity_count': 15, 'relation_count': 10, 'class_node_ratio': 0.2857, 'property_node_ratio': 0.2857, 'entity_class_ratio': 0.4286, 'entity_property_ratio': 0.2857, 'class_property_ratio': 0.1429, 'node_type_count': {'Class': 10, 'Property': 10, 'Entity': 15}, 'relation_type_count': {'属于': 1, '集成': 1, 'MENTIONS': 30, 'DOMAIN_RELATED': 10, 'HAS_PROPERTY': 10}}>>>
```

#### 样本 3

**预期输出**: `<<<{"node_count": 35, "edge_count": 45, "node_type_counts": {"Entity": 6, "Class": 29}, "relation_type_counts": {"包含": 2, "引用": 1, "控制": 2, "配置": 2, "使用": 1, "RELATED_TO": 29, "DOMAIN_RELATED": 7, "EQ...`

**graph_rl** ✅ (数值匹配, 99.5s)
```
首先，用户的问题是：“请统计该业务子图的基本信息：节点数、边数、各类型节点数量、各类型关系数量。”

我需要根据给定的图网络信息来回答这个问题。图网络的描述是JSON格式，我需要从中提取相关信息。

回顾图网络的JSON结构：
- "directed": true（表示有向图）
- "nodes": 列出所有节点，每个节点有id、labels、name、properties等。
- "edges": 列出所有边，每条边是三元组：[起点, 关系, 终点]
- "metadata": 包含"num_nodes"和"num_edges"，这可能直接给出节点数和边数。

从metadata中：
- "num_nodes": 35
- "num_edges": 45

所以，节点数是35，边数是45。

接下来，用户要求“各类型节点数量”和“各类型关系数量”。

首先，处理“各类型节点数量”：
- 节点有类型（labels）。在JSON中，每个节点有"labels"字段，是一个列表。
- 例如，节点1的labels是["Entity", "DocGraph"]，节点7的labels是["Clas...
```

**graph_sft** ❌ (不匹配, 1.2s)
```
<<<{'节点数': 35, '边数': 45, '各类型节点数量': {'Entity': 5, 'Class': 30}, '各类型关系数量': {'包含': 2, '引用': 1, '控制': 2, '配置': 2, '使用': 1, 'RELATED_TO': 30, 'DOMAIN_RELATED': 5, 'EQUIVALENT_TO': 1}}>>>
```


### 3.17 tree_check

#### 样本 1

**预期输出**: `<<<{False}>>>`

**graph_rl** ✅ (精确匹配, 74.3s)
```
首先，用户的问题是：“该业务流程是否构成树形结构（无环且连通）？”

我需要理解图网络的结构。图网络是给定的JSON数据，描述了节点和边。

关键点是：树形结构（tree）的定义是无环（acyclic）且连通（connected）的图。在图论中，树是一个连通的无向图，没有环，且有n-1条边（n个节点）。

但这里图网络是**有向图**（directed: true），因为JSON中指定了"directed": true。所以，我需要检查这个有向图是否构成树形结构。

在有向图中，树形结构通常指的是有向无环图（DAG）且连通，但树本身是无向的。问题中说“树形结构（无环且连通）”，可能指的是无向图的树，但这里图是定向的。

我需要仔细分析。

规则中提到：返回路径、节点等，但用户的问题是关于图是否构成树形结构。

根据规则，我需要先理解图网络的各个节点的内涵和关系。

从图网络描述：

- 节点有35个（num_nodes: 35）

- 边有35条（num_edges: 35）

图是**有向图**，因为"directed": true。

树形结构（tree）在有向图中通常指有向无环图...
```

**graph_sft** ❌ (不匹配, 0.4s)
```
<<<{True}>>>
```

#### 样本 2

**预期输出**: `<<<{False}>>>`

**graph_rl** ✅ (精确匹配, 15.4s)
```
首先，用户的问题是：“该业务流程是否构成树形结构（无环且连通）？”

我需要理解图网络的结构。图网络是给定的JSON数据，描述了节点和边。

关键点是：树形结构（tree）的定义是无环（acyclic）且连通（connected）的图。在图论中，树是一个连通的无向图，没有环。但这里图是定向的（directed: true），所以我要考虑有向图。

规则中提到：图网络是定向的（directed: true），所以这是一个有向图。

树形结构在有向图中通常指有向无环图（DAG）且连通。但严格来说，有向树（rooted tree）是连通的、有向无环图，且有且仅有一个根节点。

用户的问题是“是否构成树形结构（无环且连通）”，所以这里可能指的是无环且连通的有向图。

但树形结构通常隐含了有根节点，但问题中没有指定根节点。我需要检查这个图是否是无环且连通的。

首先，我需要确定图是否连通。

连通性：在有向图中，连通性通常指强连通（strongly connected）或弱连通（weakly connected）。但树形结构一般指弱连通（weakly connected）且无环。

在上下文中，...
```

**graph_sft** ❌ (不匹配, 0.2s)
```
<<<{True}>>>
```

#### 样本 3

**预期输出**: `<<<{False}>>>`

**graph_rl** ✅ (精确匹配, 66.8s)
```
首先，用户的问题是：“该业务流程是否构成树形结构（无环且连通）？”

我需要理解图网络的结构。图网络是给定的JSON数据，描述了节点和边。

关键点是：树形结构（tree）的定义是无环（acyclic）且连通（connected）的图。在图论中，树是一个连通的无向图，没有环。但这里图是定向的（directed: true），所以我要考虑有向图。

规则中提到：图网络是定向的（directed: true），所以这是一个有向图。

树形结构在有向图中通常指有向无环图（DAG）且连通。但严格来说，有向树（rooted tree）是连通的、有向无环的，且有且仅有一个根节点。

用户问题说“无环且连通”，所以这里可能指的是有向无环图（DAG）且连通。

但树形结构通常隐含了有根的结构。我需要检查这个图是否满足：

1. 无环（acyclic）：没有环（cycle）。

2. 连通（connected）：整个图是连通的（在有向图中，连通性可能指强连通或弱连通？但树形结构通常指弱连通，即忽略方向后连通）。

在图论中，对于有向图：

- 无环：没有有向环。

- 连通：可能指弱连通（weakly ...
```

**graph_sft** ❌ (不匹配, 0.4s)
```
<<<{True}>>>
```


### 3.18 tree_lca

#### 样本 1

**预期输出**: `<<<{'预算编制'}>>>`

**graph_rl** ✅ (精确匹配, 38.5s)
```
首先，我需要理解用户的问题："以'预算编制'为根节点，'ismutideviceonline'和'预算编制审批业务阶段'的最近公共上级业务对象是什么？"

根据规则，我必须先分析图网络的节点和关系，然后确定问题的类型，最后列出用于回答问题的信息。

回顾图网络的结构：
- 节点有ID、标签、名称和属性。
- 边表示节点之间的关系。
- 图是直接的（directed: true）。

关键点：
- 用户指定了根节点是"预算编制"。
- 问题涉及两个对象："ismutideviceonline"和"预算编制审批业务阶段"。
- 需要找到它们的最近公共上级业务对象。

我需要明确"ismutideviceonline"和"预算编制审批业务阶段"在图中代表什么。

从图网络描述中：
- "ismutideviceonline" 是一个属性节点。具体来说，它是一个Property节点，ID为31，标签是["Property", "DatatypeProperty"]，名称是"ismutideviceonline"。
- "预算编制审批业务阶段" 是一个类节点。具体来说，ID为21，标签是["Cl...
```

**graph_sft** ❌ (不匹配, 0.4s)
```
<<<{'YcmdApplicationParam'}>>>
```

#### 样本 2

**预期输出**: `<<<{'单据文件清单费用项目'}>>>`

**graph_rl** ✅ (精确匹配, 15.0s)
```
首先，我需要理解这个图网络的结构。根据提供的JSON数据：

- 图是定向的（directed: true）。
- 节点有6个：
  - 节点1：id=1，标签["Class"]，名称"单据文件清单费用项目"，属性{"name": "单据文件清单费用项目"}
  - 节点2：id=2，标签["Property", "DatatypeProperty"]，名称"单据文件清单表头主键"，属性{"name": "单据文件清单表头主键"}
  - 节点3：id=3，标签["Property", "DatatypeProperty"]，名称"费用项目"，属性{"name": "费用项目"}
  - 节点4：id=4，标签["Property", "DatatypeProperty"]，名称"费用项目类别"，属性{"name": "费用项目类别"}
  - 节点5：id=5，标签["Property", "DatatypeProperty"]，名称"时间戳"，属性{"name": "时间戳"}
  - 节点6：id=6，标签["Property", "DatatypeProperty"]，名称"租...
```

**graph_sft** ✅ (精确匹配, 0.5s)
```
<<<{'单据文件清单费用项目'}>>>
```

#### 样本 3

**预期输出**: `<<<{'BoardAgreement'}>>>`

**graph_rl** ❌ (不匹配, 24.5s)
```
首先，我需要理解这个图网络的结构。图网络是直接的（directed: true），有32个节点和31条边。所有节点都属于"Class"和"CoreClass"标签，每个节点都有一个名称和属性。

关键点是：所有边都是"SUB_CLASS_OF"关系。这意味着每个节点（除了根节点）都通过"SUB_CLASS_OF"关系指向其父类节点。例如，边列表显示"BoardAgreement" -> "SUB_CLASS_OF" -> "Agreement"，所以"BoardAgreement"是"Agreement"的子类。

用户的问题是：以'BoardAgreement'为根节点，'MasterAgreementEarlyTerminationRight'和'MutualContractualAgreement'的最近公共上级业务对象是什么？

我需要找到这两个节点的最近公共上级（即它们的最近共同祖先）。

由于图是树状结构（所有边都是SUB_CLASS_OF，表示类层次），这应该是一个有向无环图（DAG），但在这里，所有节点都通过SUB_CLASS_OF关系连接到"Agreement"，所以...
```

**graph_sft** ❌ (不匹配, 0.5s)
```
<<<{'Agreement'}>>>
```


## 4. 评测结论

- **最高准确率**: graph_rl (79.6%)
- **最快响应**: graph_sft (平均 0.53s)

### 各模型优势任务类型

- **graph_rl**: PREDECESSOR(100%), EDGE(100%), entity_attribute(100%), entity_relation(100%), entity_type(100%), relation_between(100%), relation_path(100%), summary(100%), tree_check(100%), MST(100%), COMMON_NEIGHBOR(100%)
- **graph_sft**: EDGE(100%), entity_attribute(100%), entity_relation(100%), entity_type(100%), relation_between(100%), relation_path(100%), COMMON_NEIGHBOR(100%)